$(document).ready(function(){

	$('#pagepiling').pagepiling({
    sectionsColor:['#fff','#fff','#fff','#fff','#fff', '#fff'],
});

  $('.slider').slick({
    slidesToShow: 1,
    dots: true,
    infinite: false,
    draggable: false
  });


});
